{
  "name": "punch2pen-cloud",
  "private": true,
  "version": "0.1.0",
  "description": "punch2pen paid-profile backend: plugin login, portable dictionaries, workspace seats",
  "type": "module",
  "scripts": {
    "dev": "convex dev",
    "codegen": "convex codegen",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --test tests/"
  },
  "dependencies": {
    "convex": "^1.46.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
