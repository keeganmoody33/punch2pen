{
  "name": "punch2pen-site",
  "private": true,
  "description": "punch2pen.com pre-customer landing page (Cloudflare Worker + static assets)",
  "scripts": {
    "check": "bash ./check.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "node --check src/worker.js"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
